APP_KEY={{ monicahq.app_key }}
DB_HOST={{ mariadb.host }}
DB_USERNAME={{ monicahq.db.username }}
DB_PASSWORD={{ monicahq.db.password }}
DB_DATABASE={{ monicahq.db.database }}
MAIL_DRIVER=stmp
MAIL_ENCRYPTION=tls
MAIL_HOST={{ smtp.host }}
MAIL_PORT={{ smtp.port }}
MAIL_USERNAME={{ smtp.username }}
MAIL_PASSWORD={{ monicahq.smtp.password }}
MAIL_FROM_ADDRESS={{ monicahq.smtp.from_address }}
MAIL_FROM_NAME={{ monicahq.smtp.from_name }}
CACHE_DRIVER=redis
SESSION_DRIVER=database
QUEUE_DRIVER=redis
REDIS_HOST={{ redis.host }}
