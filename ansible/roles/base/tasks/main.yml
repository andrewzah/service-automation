- name: ensure packages are installed
  become: yes
  apt:
    name:
      - ntp
      - neovim
      - vim
      - nfs-common
    state: present
    update_cache: yes
  when: ansible_distribution == "Debian"

- name: ensure services are started
  become: yes
  service:
    name: ntp
    state: started
